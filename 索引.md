
# MySQL 索引核心知识点

---

## 一、索引失效场景

### 1. 最左前缀法则（联合索引）

> 联合索引必须从最左列开始匹配，**不能跳过中间列**。
联合索引 (a, b, c)

✅ WHERE a = 1 — 走索引 ✅ WHERE a = 1 AND b = 2 — 走索引 ✅ WHERE a = 1 AND b = 2 AND c = 3 — 走索引（全匹配） ❌ WHERE b = 2 — 不走索引（缺少最左列 a） ❌ WHERE a = 1 AND c = 3 — 只走 a，c 失效（跳过 b）

text




> **注意**：`a` 在 WHERE 中的顺序不重要，优化器会自动调整，关键是有没有出现。

---

### 2. 范围查询导致右侧列失效

> 范围查询（`>`、`<`、`>=`、`<=`、`BETWEEN`、`LIKE 'xxx%'`）出现后，**右侧的索引列全部失效**。
联合索引 (age, name, score)

❌ WHERE age > 20 AND name = '张三' AND score = 90 → age 走索引，name 和 score 失效

❌ WHERE age >= 20 AND name = '张三'
→ age 走索引，name 失效（>= 同样是范围查询！）

text




> **规避方式**：建联合索引时，**把范围查询的字段放到最后**。
>
> ```sql
> -- ✅ 改为 (name, score, age)
> CREATE INDEX idx_n_s_a ON t(name, score, age);
> ```

---

### 3. 对索引列做函数/运算操作

```sql
-- ❌ 索引失效 SELECT * FROM t WHERE LEFT(name, 3) = 'abc'; SELECT * FROM t WHERE YEAR(create_time) = 2024; SELECT * FROM t WHERE age + 1 = 20;

-- ✅ 改成这样 SELECT * FROM t WHERE name LIKE 'abc%'; SELECT * FROM t WHERE create_time BETWEEN '2024-01-01' AND '2024-12-31'; SELECT * FROM t WHERE age = 19;

text




---

### 4. 隐式类型转换

> `varchar` 类型字段查询时**不加引号**，会发生隐式类型转换，导致索引失效。

```sql
-- phone 是 varchar 类型 -- ❌ 索引失效（MySQL 会把字符串转成数字） SELECT * FROM t WHERE phone = 13800138000;

-- ✅ 加引号 SELECT * FROM t WHERE phone = '13800138000';

text




---

### 5. 模糊匹配 LIKE

| 写法 | 是否走索引 |
|------|-----------|
| `LIKE '软件%'` | ✅ 走索引（后模糊） |
| `LIKE '%工程'` | ❌ 不走索引（前模糊） |
| `LIKE '%软件%'` | ❌ 不走索引（全模糊） |

---

### 6. OR 条件

> `OR` 连接的查询条件中，**所有字段都必须有索引**，才会走索引。

```sql
-- 假设 name 有索引，age 无索引 -- ❌ 不走索引 SELECT * FROM t WHERE name = '张三' OR age = 20;

-- ✅ 两个字段都有索引 SELECT * FROM t WHERE name = '张三' OR id = 1;

text




---

### 7. `!=` / `<>` / `NOT IN`

> 这些操作**通常**不走索引，但最终取决于数据分布。
>
> 如果匹配的数据量很小，MySQL 可能走索引；如果匹配的数据量很大，优化器会选择全表扫描。

---

### 8. 数据分布影响

> 当优化器判断**全表扫描比索引更快**时，会自动放弃索引。
数据量小（如几十行） → 全表扫描 查询结果占大部分数据 → 全表扫描 查询结果占极少部分数据 → 走索引

text




---

## 二、SQL 提示（Index Hint）

> 当某个字段有多个索引时，可以手动指定使用哪个索引。

| 提示 | 作用 | 强制程度 |
|------|------|----------|
| `USE INDEX(idx_name)` | 建议使用某索引 | ⭐ 弱，优化器可能不采纳 |
| `IGNORE INDEX(idx_name)` | 忽略某索引 | ⭐⭐ 中 |
| `FORCE INDEX(idx_name)` | 强制使用某索引 | ⭐⭐⭐ 强，优化器基本必采纳 |

```sql
SELECT * FROM t USE INDEX(idx_name) WHERE name = '张三'; SELECT * FROM t FORCE INDEX(idx_name) WHERE name = '张三';

text




> **`USE` vs `FORCE`**：`USE` 只是建议，SQL 优化器会自行判断是否使用；`FORCE` 是强制，优化器基本会遵守。

---

## 三、覆盖索引

> 查询的**所有列都在索引中**，不需要回表去聚集索引拿数据。

| Extra 值 | 含义 |
|----------|------|
| `Using index` | ✅ 覆盖索引，不需要回表，性能最优 |
| `Using index condition` | ⚠️ 索引下推（ICP），在索引层先过滤，但可能仍需回表 |
| `NULL` | ❌ 没有使用索引优化，纯回表查询 |

```sql
-- 联合索引 (name, age)

-- ✅ Using index（覆盖索引，查的列都在索引里） SELECT name, age FROM t WHERE name = '张三';

-- ⚠️ Using index condition（ICP，score 不在索引中，需要回表） SELECT name, age, score FROM t WHERE name = '张三' AND age > 20;

text




> **最佳实践**：查询时避免 `SELECT *`，尽量只查需要的列，利用覆盖索引避免回表。

---

## 四、前缀索引

> 当字段是长字符串时，对整个字段建索引会很大，浪费 IO。可以只取**前面一部分字符**建立索引。

```sql
-- 取前 n 个字符建立前缀索引 CREATE INDEX idx_name ON table_name(column_name(n));

text




### 前缀长度 n 的选择
选择性 = COUNT(DISTINCT SUBSTRING(column, 1, n)) / COUNT(*)

text




> 选择性越接近 **1** 越好，说明前 n 个字符区分度足够高。

```sql
-- 计算前 9 个字符的选择性 SELECT COUNT(DISTINCT SUBSTRING(column, 1, 9)) / COUNT(*) FROM t;

text




---

## 五、单列索引 vs 联合索引

| | 单列索引 | 联合索引 |
|------|----------|----------|
| 回表概率 | 高（经常需要回表） | 低（更容易覆盖索引） |
| 多条件查询 | 每次只用一个索引 | 多个条件一起优化 |
| 推荐度 | ⭐⭐ | ⭐⭐⭐（推荐） |

---

## 六、索引设计原则

1. **数据量较大**（百万级）且**查询频繁**的表才建索引
2. **优先使用联合索引**，而非多个单列索引
3. 对 `WHERE`、`ORDER BY`、`GROUP BY` 后面的字段建索引
4. **控制索引数量**：索引不是越多越好，会拖慢增删改速度
5. 字段允许 `NULL` 时，用 `NOT NULL` 约束（`NULL` 值在索引中处理特殊）
6. **区分度高的字段**优先建索引（如身份证号 > 性别）
7. 频繁更新的字段**不适合**建索引

---

## 七、索引失效速查表

| 序号 | 场景 | 是否失效 |
|------|------|----------|
| 1 | 不满足最左前缀 | ❌ 失效 |
| 2 | 范围查询右侧的列 | ❌ 失效 |
| 3 | 对索引列做函数/运算 | ❌ 失效 |
| 4 | 隐式类型转换 | ❌ 失效 |
| 5 | `LIKE '%xxx'` 前模糊 | ❌ 失效 |
| 6 | `LIKE 'xxx%'` 后模糊 | ✅ 走索引 |
| 7 | `OR` 两边不全有索引 | ❌ 失效 |
| 8 | `!=` / `<>` / `NOT IN` | ⚠️ 通常不走路 |
| 9 | `IS NULL` / `IS NOT NULL` | ⚠️ 看数据分布 |
| 10 | 全表扫描更快 | ❌ 优化器放弃索引 |

---

## 八、记忆口诀
最左前缀不能少，跳过左边全完了 范围之后全失效，把范围放最后就好 函数运算别乱搞，引号类型要配套 前模糊不走后模糊走，OR 两边都要有 覆盖索引防回表，数据太少全表扫